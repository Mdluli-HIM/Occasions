// Provider dashboard data now comes from GET /api/providers/me/stats,
// GET /api/providers/me/leads and GET /api/providers/me/settings
// (see src/lib/api.ts and src/app/provider-dashboard/**). Kept empty so any
// stray import doesn't hard-crash the build.
export {};
