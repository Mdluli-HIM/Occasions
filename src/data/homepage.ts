// Homepage categories and featured providers now come from
// GET /api/categories/services, GET /api/categories/occasions and
// GET /api/providers/featured (see src/lib/api.ts and the components under
// src/components/home/). This file is intentionally empty — kept so any
// stray import doesn't hard-crash the build; prefer @/types/homepage for types.
export {};
